# awareness_splash0r
Windows fullscreen awareness splash app in C#/.NET that shows images on all monitors, overlays a configurable countdown, switches to an optional second image shortly before time runs out, blocks Alt+F4 and Windows keys, and auto-closes when the timer reaches zero.
